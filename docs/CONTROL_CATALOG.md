# Control Catalog — Milestone 1

This catalog describes the deterministic native controls shipped in the first build milestone. The controls intentionally target high-confidence patterns in bounded repository evidence. They do **not** prove that a deployed application is secure, complete, compliant, or free of business-logic vulnerabilities.

| Control | Default profile action | Evidence inspected | Milestone-1 detection boundary | Next adapter direction |
|---|---|---|---|---|
| `SEC-SECRET-001` | Block | Bounded text files in the working tree. | Private-key markers, selected provider token shapes, and high-confidence secret assignments. The suspected value is never written to reports. | Gitleaks with explicitly approved history scope. |
| `SEC-SAST-001` | Block | Parseable Python source. | SQL f-string, `%` formatting, and `.format()` passed directly to `execute`/`executemany` or through one local straight-line simple-name assignment. It does not follow branches, aliases, calls, imports, object state, or interprocedural flow. | Separate contracts for one local alias, then reviewed framework-specific AST rules. |
| `SEC-SAST-SQL-ALIAS-001` | Dedicated `python-sql-single-alias-policy.yaml` only | Parseable Python source. | One same-scope ordinary name-to-name alias from an already unsafe local SQL query into a standalone `execute`/`executemany` sink. It excludes alias chains, branches, loops, calls, imports, attributes, subscripts, annotations, closures, wrapped sinks, parameter binding, and runtime behavior. | One separately reviewed flow shape at a time; no general dataflow claim. |
| `SEC-API-001` | Block | Parseable FastAPI route decorators and declared dependencies. | Supported static mutating routes without a visible `Depends`/`Security` dependency or exact public-route allowlist entry create findings. Dynamic paths, `api_route` methods, and the direct module-top-level non-literal `APIRouter(prefix=...)` shape used by that same router name create a `REVIEW_REQUIRED` execution state only, never a finding or gate change. Prefix values and effective paths are not derived; dependency presence is not proof of correct authorization. | Dependency/data-flow and policy-aware route analysis. |
| `SEC-API-AUTHZ-001` | Dedicated `fastapi-authorization-policy.yaml` only | Parseable FastAPI route decorators and direct dependency defaults. | A literal mutating route with an authentication-shaped dependency but no fixed authorization marker creates one finding. It does not prove roles, scopes, ownership, tenant isolation, dependency semantics, middleware, wrappers, or runtime enforcement. | Reviewed role/scope/ownership contracts only after independent calibration. |
| `SEC-DATA-INTEGRITY-001` | Dedicated `python-data-integrity-policy.yaml` only | Parseable Python source and direct literal SQL strings. | Direct literal `UPDATE` or `DELETE` passed to standalone `execute`/`executemany` without `WHERE` creates one finding. Formatted SQL, variables, aliases, ORM methods, migrations, schemas, transactions, SQL semantics, and runtime behavior are excluded. | Separate reviewed transaction, constraint, and ORM contracts only after independent calibration. |
| `SEC-API-UPLOAD-001` | Dedicated `fastapi-file-upload-policy.yaml` only | Parseable Python source with direct FastAPI upload routes. | A direct `UploadFile.filename` expression passed to built-in `open` in a literal mutating route creates one finding. Sanitization, path resolution, storage behavior, archive safety, MIME validation, size limits, malware scanning, authorization, aliases, and runtime behavior are excluded. | Separate upload-content, storage, and archive-safety contracts only after independent calibration. |
| `SEC-CONFIG-001` | Block | Python and selected configuration files. | Explicit `DEBUG=True` or `DEBUG=true` declarations. It does not establish the deployed effective configuration. | Deployment and runtime evidence adapters. |
| `SEC-CONFIG-002` | Block | FastAPI middleware calls and selected configuration files. | Wildcard origins combined with credentialed CORS. | Framework and runtime configuration adapters. |
| `SEC-NEXT-ENV-001` | Block when Next.js is detected | Direct `process.env.NEXT_PUBLIC_*` references in bounded JS/TS source. | Names that clearly indicate a secret, private credential, password, token, or session are rejected. It does not evaluate values, computed lookups, server-only variables, or data flow. | TypeScript AST/data-flow rules and client-boundary analysis. |
| `SEC-NEXT-COOKIE-001` | Block when Next.js is detected | Direct static `cookies().set(...)` or cookie-store setter calls in bounded JS/TS source. | Explicit insecure `httpOnly`, `secure`, or `sameSite` options on a statically named session/auth/token cookie. It does not infer omitted options or inspect wrappers. | TypeScript AST analysis, cookie-wrapper models, and route context. |
| `SEC-NEXT-CORS-001` | Block when Next.js is detected | Static `headers: [...]` blocks in `next.config.*`. | Credentialed wildcard CORS header pairs with literal values. It does not inspect middleware, proxies, environment interpolation, or runtime header generation. | Runtime/proxy evidence and framework-aware configuration analysis. |
| `SEC-NEXT-ACTION-001` | Block when Next.js is detected | Named exported async functions inside a module-level `use server` file. | A direct `db`/`prisma` mutation before a recognized local guard marker. Proxy/middleware presence is structural metadata only; it does not prove authorization, ownership, route coverage, imported delegates, aliases, closures, or runtime dataflow. | Separate contracts for broader syntax, semantic authorization, ownership, route matching, and runtime behavior. |
| `SEC-NEXT-INLINE-ACTION-001` | Dedicated `nextjs-inline-server-actions-policy.yaml` only | Named nested async functions with inline `use server`. | A direct `db`/`prisma` mutation before a recognized local guard marker. Arrow actions, module-level/exported actions, directives after executable code, page checks, proxy/middleware, aliases, helpers, and runtime behavior are excluded. | Separately reviewed arrow/closure forms and semantic authorization only after their own calibration. |
| `SEC-COMPOSE-PRIVILEGED-001` | Dedicated `docker-compose-privileged-policy.yaml` only | Supported root Compose filenames with a direct services mapping and direct service mapping. | An unquoted lowercase `privileged: true` scalar creates one finding. Dynamic/reused YAML, include/extends/profiles, other settings, images, containers, host/runtime state, and Docker/Compose execution are excluded. | Separate bounded checks for other Compose properties and other artifact families only after their own contracts. |
| `SEC-CICD-001` | Block when run | GitHub workflow YAML. | `write-all`, privileged workflow triggers that also check out repository content, and third-party actions without full-SHA pins. | GitHub policy API and OpenSSF Scorecard. |
| `SEC-DEP-001` | Block | Root dependency manifests and lockfiles. | Missing recognized Python or Node lockfile. It does not inspect CVEs or package integrity yet. | pip-audit, npm audit, OSV, lockfile integrity verification. |
| `SEC-PHP-LARAVEL-COMPOSER-LOCK-001` | Dedicated `php-laravel-composer-lock-policy.yaml` only | Root `composer.json`, root `artisan`, and root `composer.lock` presence. | A root JSON `require` object with the exact `laravel/framework` key plus root `artisan`, but no root `composer.lock`, creates one finding. The dependency value and lock content are not parsed. It does not execute PHP, Composer, or Artisan. | Separate Composer integrity or vulnerability evidence only after an independently bounded contract. |
| `SEC-RUST-CARGO-LOCK-001` | Dedicated `rust-cargo-lock-policy.yaml` only | Root `Cargo.toml`, conventional root-relative `src/main.rs`, and root `Cargo.lock` presence. | A direct non-empty `dependencies` table plus `src/main.rs`, but no root `Cargo.lock`, creates one finding. Dependency values and lock content are not parsed. It does not execute Cargo, rustc, or Rust code. | Separate Cargo integrity or vulnerability evidence only after an independently bounded contract. |
| `SEC-RUBY-RAILS-GEMFILE-LOCK-001` | Dedicated `ruby-rails-gemfile-lock-policy.yaml` only | Root `Gemfile`, conventional root-relative `config/application.rb`, and root `Gemfile.lock` presence. | An unindented literal `gem 'rails'` or `gem "rails"` declaration plus `config/application.rb`, but no root `Gemfile.lock`, creates one finding. Gemfile values and lock content are not parsed. It does not execute Ruby, Bundler, or Rails. | Separate Bundler integrity or vulnerability evidence only after an independently bounded contract. |
| `SEC-GO-MODULE-001` | Block when a root Go module is detected | Root `go.mod` and `go.sum`. | A root `go.mod` with direct or block-form dependency declarations must have a root `go.sum`. It does not inspect nested modules, checksum contents, resolved dependencies, or vulnerabilities. | Go dependency and vulnerability evidence adapters. |
| `SEC-GO-TLS-001` | Block when a root Go module is detected | Bounded Go source. | Direct `tls.Config` composite literals explicitly setting `InsecureSkipVerify: true`; comments, strings, aliases, computed values, custom verification, and runtime configuration are excluded. | Go AST/data-flow and transport-runtime evidence. |
| `SEC-GOSEC-001` | Opt-in external-adapters profile only | Preinstalled Gosec JSON report for a detected root Go module. | Fixed local arguments, module-network disabled, read-only module behavior, inline suppressions ignored, and source/details/suppression text discarded. It does not install Gosec, use AI-fix mode, download dependencies, or run target-supplied configuration. | Additional reviewed Go scanner capabilities after fixture calibration. |
| `SEC-RELEASE-001` | Strict-release profile only | CycloneDX JSON candidate files. | Missing or non-CycloneDX JSON SBOM. | CycloneDX generation, SBOM integrity, and provenance verification. |

## Detector health

An adapter failure is recorded as `ERROR`. Gosec also returns `ERROR` if its preinstalled binary is absent, its report is malformed, local module analysis fails, or it reports compiler/scanner errors; the adapter does not download dependencies or recover by enabling a network.  When a policy marks that control as required, the release outcome is `ERROR` rather than a pass. A control that cannot apply because its technology is absent is `NOT_APPLICABLE`; it is visibly recorded and is not counted as a pass.

## False-positive process

A finding may be fixed, accepted temporarily by a narrowly scoped waiver, or used to improve a reviewed rule. A waiver requires the exact finding fingerprint, rule, repository digest, accountable approver, justification, compensating controls, and future expiry. It cannot suppress a control-execution error.
